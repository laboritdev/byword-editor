# UI Tests

UI tests for window management and menu flows are planned for a future pass.

Run the app from Xcode (`open Package.swift` → ⌘R) to validate manually:

- New / Open / Save
- Toggle Preview and Focus Mode
- Preferences
- Find / Replace
